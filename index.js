/**
 * @format
 */

import {AppRegistry} from 'react-native';
import offerspageNavigator from './foodApp/navigator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => offerspageNavigator);
